# Runtime dependencies. Azure Functions installs these during deployment.
azure-functions>=1.21.0
requests>=2.32.0
python-dotenv>=1.0.0
# Windows ships no system timezone database, so zoneinfo needs this. Without
# it every timestamp silently falls back to UTC and the daily re-alert fires
# at the wrong hour.
tzdata>=2024.1
# State storage in Azure. Imported lazily, so local runs that use the JSON
# file store never load it.
azure-data-tables>=12.5.0
azure-identity>=1.17.0
